{% extends 'base.html.twig' %}

{% block title %}Hello ParfumController!{% endblock %}

{% block body %}
<div class ='parfum'>

      <div class="image-detail-parfum">
      {% set imagePath = asset("" ~ parfum.image) %}
      <img src="{{ imagePath }}">
      </div>

      <div class='info-parfum'>
           <br> <h1>{{parfum.nom}}</h1>
           <a href="{{path('detail_marque',{"id":marque.id})}}"> {{parfum.marque}} </a><br>

            Gamme de Prix: <br>
            
            <br>Note de Tête:<br>
            {% for noteTete in parfum.noteTete %}
            {{ noteTete.nom }}<br>
            {% endfor %}
            
            <br>Note de Coeur:<br>
            {% for noteCoeur in parfum.noteCoeur %}
            {{ noteCoeur.nom }}<br>
            {% endfor %}
            
            <br>Note de Fond:<br>
            {% for noteFond in parfum.noteFond %}
            {{ noteFond.nom }}<br>
            {% endfor %}
      </div>
</div>

{% endblock %}